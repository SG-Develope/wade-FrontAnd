# WADE 최종발표 대비 정리

> 낙동강 실시간 수위 모니터링 서비스 — 프론트(wade) + 백엔드(wade-api) 1인 풀스택 프로젝트
> 발표 전 "기술스택·버전·이유 / 로직 흐름 / 코드 동작방식 / 예상질문"을 한 문서에 정리했습니다.

---

## 0. 한 문장 요약 (엘리베이터 피치)

낙동강홍수통제소가 공개하는 **날것의 수위 숫자(예: "양포교 3.7m")** 를, 강변 캠핑·낚시·자전거·산책 이용자가 이해할 수 있는 **"지금 가도 되는지"라는 장소 기반 안전 정보**로 번역해주는 실시간 서비스입니다. 기존 조기경보는 기관이 판단해 문자를 보내야 시민이 알 수 있었지만, WADE는 시민이 **능동적으로** 현재 상황을 확인하고 스스로 판단하게 합니다.

---

## 1. 기술 스택 — 무엇을, 어떤 버전으로, 왜

### 1-1. 프론트엔드 (wade)

| 기술 | 버전 | 왜 이걸 썼나 (발표용 답변) |
|---|---|---|
| **React** | 19 | 컴포넌트 기반 UI. 대시보드/차트/지도처럼 상태에 따라 자주 바뀌는 화면에 적합. |
| **TypeScript** | 6.x | 외부 공공 API 응답 구조가 복잡 → 타입으로 응답 형태를 고정해 런타임 오류를 컴파일 단계에서 잡음. `strict` 모드 사용. |
| **Vite** | 8 | 빠른 개발 서버(HMR)와 빌드. CRA보다 가볍고 빠름. `@` → `src` 경로 alias 설정. |
| **Tailwind CSS** | v4 | 유틸리티 클래스로 빠르게 반응형 UI 작성. v4는 `@tailwindcss/vite` 플러그인으로 별도 config 없이 동작. |
| **TanStack Query** | v5 | **서버 상태** 관리 핵심. 캐싱 + 자동 폴링(refetchInterval) + 로딩/에러 상태를 선언적으로 처리. 수위는 10분마다 자동 갱신. |
| **Zustand** | v5 | **클라이언트 상태**(선택된 관측소, 모달 열림 등) 관리. Redux보다 보일러플레이트가 적은 경량 스토어. |
| **React Router** | v7 | SPA 라우팅. 4개 페이지(대시보드/날씨/추이/여가). |
| **Recharts** | v3 | 24시간 수위 변화 라인 차트. |
| **hls.js** | 1.6 | CCTV 실시간 영상(HLS `.m3u8` 스트림)을 브라우저에서 재생. |
| **axios** | 1.x | HTTP 클라이언트. 인터셉터로 요청/응답 로깅·에러 처리 공통화. |

> **핵심 개념 — "서버 상태 vs 클라이언트 상태 분리"**
> 이 질문 나오면 만점 답변: *"API에서 온 데이터(수위·날씨)는 TanStack Query가, 순수 UI 상태(모달·선택값)는 Zustand가 관리하도록 역할을 나눴다."*

### 1-2. 백엔드 (wade-api)

| 기술 | 버전 | 왜 |
|---|---|---|
| **Spring Boot** | 4.0.7 | REST API 서버. 외부 공공 API를 프록시/가공하고 DB를 관리. |
| **Java** | 21 (toolchain) | LTS. `switch` 표현식, record, `var` 등 최신 문법 사용. |
| **Gradle** | — | 빌드 도구. `io.spring.dependency-management`로 버전 관리. |
| **MyBatis** | 4.0.1 | SQL을 XML 매퍼에 직접 작성 → 쿼리를 완전히 제어. JPA 대신 선택(단순 조회 위주라 SQL 직접 관리가 명확). |
| **Caffeine Cache** | — | 인메모리 캐시. AI 브리핑 결과를 10분 캐싱해 외부 AI API 호출·비용 절감. |
| **PostgreSQL (Neon)** | — | 서버리스 Postgres. 관측소·CCTV·장소·격자좌표 등 **메타데이터** 저장. |
| **Lombok** | — | `@Data`, `@RequiredArgsConstructor`로 보일러플레이트 제거. |
| **Jackson** | — | 외부 API JSON 응답 파싱(`ObjectMapper`, `JsonNode`). |
| **spring-boot-devtools** | — | 개발 중 자동 재시작. |

> **DB에 저장되는 것 vs 실시간으로 가져오는 것** (자주 나올 질문)
> - **DB(정적 메타데이터):** 관측소 기준수위(관심/주의/경보/심각/계획홍수위), CCTV 위치, 여가 장소, 기상청 격자좌표(nx/ny)
> - **실시간(외부 API):** 현재 수위, 수위 이력, 날씨, 레이더/위성/태풍, AI 브리핑, CCTV 스트림 URL
> 즉, **"자주 안 바뀌는 건 DB, 계속 바뀌는 건 API"** 로 나눴습니다.

### 1-3. 외부 API (데이터 출처)

- **낙동강홍수통제소 (HRFCO, api.hrfco.go.kr)** — 수위 현재값/이력 (10분·1시간 간격)
- **기상청 (공공데이터포털)** — 초단기실황·초단기예보·단기예보·특보·레이더·위성·태풍
- **국가교통정보센터 ITS (openapi.its.go.kr)** — 도로 CCTV 실시간 HLS 스트림
- **Google Gemini (gemini-2.0-flash)** — AI 안전 브리핑 자연어 생성 ⚠️(README엔 "Claude"로 적혀있음 — 아래 gotcha 참고)

### 1-4. 배포

- **프론트:** `netlify.toml` 존재 (Netlify, SPA 리다이렉트 설정). *README엔 Vercel로 적혀있음 — 실제 설정은 Netlify.*
- **백엔드:** `Procfile` 존재 (`java -jar ... --server.port=$PORT`) → Railway 계열 PaaS.

---

## 2. 시스템 아키텍처 & 데이터 흐름

```
[사용자 브라우저]
    │
    │  ① 정적 메타데이터 (/api/stations, /api/places, /api/cctv, /api/weather/*, /api/ai/*)
    ▼
[React SPA] ──────────────► [Spring Boot (wade-api)] ──► [PostgreSQL / Neon]  (관측소·CCTV·장소·격자)
    │                              │
    │                              ├──► 기상청 API      (날씨·레이더·위성·태풍)
    │                              ├──► Gemini API      (AI 브리핑, 10분 캐시)
    │                              └──► ITS API         (CCTV 최신 스트림 URL)
    │
    │  ② 실시간 수위는 프론트가 HRFCO를 "직접" 호출 (아래 주의)
    └──────────────────────────────────────────────► [HRFCO api.hrfco.go.kr]
```

### ⚠️ 꼭 알아야 할 아키텍처 포인트: 수위 데이터는 두 경로가 공존

- 백엔드에도 `WaterLevelService`(`/api/water-levels/current`, `/history`)가 **구현돼 있음**.
- 하지만 **실제 프론트(`src/api/waterLevel.ts`)는 백엔드를 거치지 않고 HRFCO를 직접 호출**합니다.
  - 관측소 **메타데이터(기준수위, obsCode)** 만 백엔드 `/api/stations`에서 받고,
  - **현재 수위·이력 숫자**는 브라우저에서 `api.hrfco.go.kr`로 직접 `axios.get`.
- 왜 이렇게? → 백엔드 배포 서버 IP가 HRFCO에 차단될 수 있고(코드 주석에 "IP 차단 가능"), 브라우저에서 직접 부르면 사용자별 IP로 분산됨.
- **면접 대비:** "왜 수위만 프론트에서 직접 부르냐" 물으면 → *"HRFCO는 서버 IP 기준 요청 제한/차단 이슈가 있어, 메타데이터만 백엔드에서 받고 실시간 수치는 클라이언트에서 직접 조회하도록 분리했다. 백엔드에도 동일 로직을 fallback으로 남겨뒀다."*

---

## 3. 백엔드 레이어별 동작 방식

**표준 Spring 3계층:** Controller(HTTP) → Service(비즈니스 로직/외부 API) → Mapper(MyBatis SQL) → DB

### 3-1. 설정 (config)

- **`CacheConfig`** — Caffeine 기반. 캐시 이름 `aiGuide`, `cctvUrls`. **10분 만료(expireAfterWrite), 최대 50개**. `@EnableCaching`은 메인 클래스에.
- **`CorsConfig`** — `/api/**` 경로에 CORS 허용. `allowedOriginPatterns("*")`로 모든 오리진 허용(개발 편의).
- **`RestTemplateConfig`** — ⭐중요⭐ 두 가지:
  1. `@PostConstruct`에서 **SSL 인증서 검증을 전역 비활성화**. 이유: 한국 공공기관 API(hrfco 등)가 인증서 체인 문제로 `PKIX path building failed` 오류를 자주 냄. 이를 우회.
  2. `RestTemplate` Bean 생성 — connect 10초 / read 15초 타임아웃.
  - **주의(보안 질문 대비):** SSL 검증 끄는 건 실무에선 위험 → 발표 땐 *"공공 API 인증서 이슈를 넘기려 임시로 껐고, 운영에선 신뢰 저장소에 인증서를 등록하는 방식으로 바꿔야 한다"* 라고 답하면 좋음.

### 3-2. 수위 서비스 (`WaterLevelService`)

- **현재 수위:** DB 관측소 목록 순회 → 각 obsCode로 HRFCO `.../waterlevel/list/10M/{obsCode}.json` 호출 → `content[0].wl` 파싱 → 상태 계산.
- **이력:** `hours ≤ 6`이면 10분 간격(`10M`), 초과면 1시간 간격(`1H`) API 사용. 시간은 **Asia/Seoul** 기준, 10분 단위로 내림 정렬 후 조회. 결과는 최신→과거 순이라 `Collections.reverse()`로 뒤집어 시간순 정렬.
- **상태 계산 `calcStatus`:** `심각(serious) → 경보(alarm) → 관심(attention)` 순으로 비교. 데이터 못 받으면 `-1` 반환 후 `unknown` 처리.

### 3-3. 날씨 서비스 (`WeatherService`) — 가장 복잡

기상청 API 7종을 감쌈. 핵심은 **base_time(발표시각) 계산 로직**:

- **초단기실황(Ncst):** 매시 정시 발표, 10분 후 제공 → 현재 분<10이면 이전 시간 사용.
- **초단기예보(UltraSrtFcst):** 매시 30분 발표, 45분 후 조회 가능 → 분<45면 이전 시간의 `HH30`.
- **단기예보(VilageFcst):** 하루 8회(02·05·08·11·14·17·20·23시) 발표 → 현재 시각 이하의 가장 최근 발표 시각 선택.
- **격자좌표:** 위경도가 아니라 기상청 전용 격자(nx/ny) 사용. DB `weather_grid`에서 관측소별 매핑(구미 86/96, 칠곡 85/93). 없으면 기본값.
- **응답 파싱:** 같은 시각의 여러 category(TMP·SKY·POP·PTY 등)를 `날짜+시각`을 key로 묶어 한 객체로 조립(`LinkedHashMap` + 빌더).
- **에러 처리:** 429(요청 한도 초과)는 별도로 잡아 사용자 메시지로 변환. `GlobalExceptionHandler`에서도 전역 처리.
- 코드값 → 한글 변환 헬퍼: `parseSky`(1맑음/3구름많음/4흐림), `parsePrecipType`(0없음~7눈날림), `parseWindDir`(각도→8방위).

### 3-4. AI 브리핑 서비스 (`AiGuideService`) — 발표 하이라이트

1. 프론트가 현재 수위(`{yangpo, hoguk}`)를 POST로 보냄.
2. **캐시 키 생성:** 수위를 0.1m 단위로 반올림해 키로 사용 → 수위가 거의 같으면 같은 캐시 사용(`@Cacheable`). **10분간 동일 요청은 AI 재호출 안 함** → 비용/속도 최적화.
3. **프롬프트 조립:** 각 관측소 현재 수위, 단계, 기준수위, 관심수위 대비 차이(`+X.XXm`), **체감 높이 라벨**("버스 지붕 높이" 등)을 넣어 Gemini에 전달. JSON만 응답하도록 지시.
4. **응답 파싱:** 마크다운 코드펜스(```) 제거 후 `{...}` 부분만 추출해 JSON 파싱 → `message` + `activities`(산책/낚시/자전거/캠핑 가능여부).
5. **Fallback:** AI 키 없거나 호출 실패 시 **규칙 기반(rule-based) 메시지** 생성(정상/주의/경보/심각 4단계별 템플릿). → **AI가 죽어도 서비스는 계속 동작**. (좋은 설계 포인트로 어필 가능)

- **`heightLabel`(체감 높이):** 수위 숫자를 "성인 허리 높이", "버스 지붕 높이"처럼 직관적으로 번역. 이게 이 서비스의 정체성("숫자를 안전 정보로 번역")을 코드로 구현한 부분.

### 3-5. CCTV 컨트롤러 (`CctvController`)

- DB에서 CCTV 목록 조회 후, **ITS API로 최신 스트림 URL을 실시간 갱신**(`enrichWithFreshUrl`). ITS 스트림 URL은 시간이 지나면 만료되므로 매번 새로 받아야 함.
- CCTV들의 위경도로 bounding box를 만들어 ITS에 질의 → 이름(`cctvname`)이 일치하는 항목의 `cctvurl`로 교체.
- 실패해도 DB에 저장된 URL로 fallback.

### 3-6. 매퍼 (MyBatis)

- `StationMapper`, `CctvMapper`, `PlaceMapper`, `StationMetaMapper`, `WeatherGridMapper` + 각 `.xml`.
- `schema.sql`로 테이블 생성, `data.sql`로 초기 데이터 삽입(`spring.sql.init.mode=always`, `ON CONFLICT ... DO UPDATE`로 재실행 안전).

---

## 4. 프론트엔드 동작 방식

### 4-1. 진입 & 구조

- `main.tsx`: `QueryClientProvider`(retry 1, staleTime 10분) + `RouterProvider`로 앱 감쌈.
- `App.tsx`: 상단 네비(데스크톱) / 하단 탭바(모바일) **반응형**. `<Outlet/>`에 페이지 렌더.
- `router/index.tsx`: `/dashboard`, `/weather`, `/trends`, `/leisure` 4개 라우트. 루트는 dashboard로 리다이렉트.

### 4-2. 데이터 페칭 패턴 (핵심)

`api/*.ts`(fetch 함수) → `queries/*.ts`(useQuery 훅) → 컴포넌트에서 훅 호출.

- **queryKeys.ts:** 쿼리 키를 한 곳에서 계층적으로 관리(캐시 무효화·충돌 방지 목적).
- **폴링 주기:** 수위/AI/현재날씨 = **10분**, 단기예보/특보 = 1시간, 레이더 = 5분, 위성 = 10분, 태풍 = 30분. (데이터 갱신 주기에 맞춤)
- **`placeholderData: (prev) => prev`:** 리페치 중에도 이전 데이터를 보여줘 화면 깜빡임 방지.

### 4-3. 대시보드 흐름 (`Dashboard.tsx`)

1. `useWaterLevels()`로 관측소+현재수위 가져옴.
2. 수위들을 `{id: level}` 맵으로 만들어 `useAiGuide()`에 전달 → AI 브리핑 요청.
3. 위험 관측소가 하나라도 있으면 상단에 **`DangerBanner`** 자동 노출.
4. `KakaoMap`에 관측소·CCTV 마커 렌더(상태별 색상). CCTV 클릭 → `CctvModal`.
5. 우측 `RightPanel`에 수위·날씨·AI 브리핑 카드.

### 4-4. CCTV 영상 재생 (`HlsPlayer.tsx`)

- `hls.js`로 `.m3u8`(HLS) 스트림을 `<video>`에 연결. `Hls.isSupported()`면 hls.js, 아니면(사파리) 네이티브 재생.
- ITS의 `http://` URL을 `https://`로 바꿔 mixed-content 차단 회피.
- 치명적 에러 시 `onError` 콜백 → 상위에서 대체 UI.

### 4-5. 상태 관리 (`stores/appStore.ts`)

Zustand 스토어: 선택된 관측소, 활성 탭, 선택된 지도 마커 + 모달 열고 닫기. 순수 UI 상태만 담당.

---

## 5. 핵심 도메인 로직 — 수위 → 안전등급

### 5-1. 관측소 기준수위 (DB `station` 테이블, `data.sql`)

| 관측소 | obsCode | 관심 | 주의보 | 경보 | 심각 | 계획홍수위 |
|---|---|---|---|---|---|---|
| 양포교(구미) | 2011625 | 3.0 | 4.0 | 5.1 | 6.17 | 6.17 |
| 호국의다리(칠곡) | 2011650 | 8.46 | 11.6 | 13.1 | 15.81 | 15.81 |

### 5-2. 상태 5단계

정상(초록) → 관심(파랑) → 주의(노랑) → 위험(빨강) → 심각(진빨강). 마커 색·배지·텍스트 색이 상태에 연동.

### 5-3. 장소 안전도

각 여가 장소는 연결된 관측소 수위 + 장소별 `safe_wl`/`caution_wl` 임계값으로 안전/주의/이용금지를 판단(`getPlaceStatus`).

---

## 6. ⚠️ 예상 함정 질문 (Gotcha) — 미리 답 준비

발표 심사에서 "코드 실제로 봤냐"를 확인하는 날카로운 질문들입니다. **정직하게 인정 + 개선방향 제시**가 정답.

1. **"AI가 Claude라고 했는데 코드는 Gemini네요?"**
   → 맞습니다. README/기술스택 표엔 Claude(Anthropic)로 적혀있지만 **실제 구현은 Google Gemini(`gemini-2.0-flash`)** 입니다. 개발 중 API 접근성 문제로 교체했고 문서 갱신이 누락됐습니다.

2. **"배포는 Vercel/Railway라면서 `netlify.toml`이 있네요?"**
   → 프론트는 **실제로 Netlify**로 배포했습니다(SPA 리다이렉트 설정 포함). README 표기가 최신화되지 않았습니다.

3. **"수위는 백엔드가 주는 거 아닌가요? 왜 프론트가 HRFCO를 직접 부르죠?"**
   → 2장 참고. HRFCO의 서버 IP 차단 이슈 회피 + 메타데이터/실시간값 분리. 백엔드에도 동일 로직을 fallback으로 보유.

4. **"SSL 인증서 검증을 전역으로 끄셨는데 보안 문제 아닌가요?"**
   → 공공 API의 인증서 체인 오류(PKIX) 회피용 임시 조치. 운영에선 truststore에 인증서 등록 방식으로 전환해야 함(인지하고 있음).

5. **"API 키가 코드/설정에 하드코딩돼 있네요?"**
   → `application-local.properties`와 `waterLevel.ts` fallback에 키가 노출돼 있습니다. **발표 후 즉시 조치 권장:** 환경변수/시크릿으로 분리하고 노출된 키는 폐기·재발급. (지금 이 저장소는 git에 키가 올라가 있으므로 실제로 로테이션 필요)

6. **"상태 판단 기준이 서비스마다 다르지 않나요?"**
   → 실제로 `WaterLevelService.calcStatus`(주의 기준=관심수위)와 `AiGuideService.calcStatus`(주의 기준=주의보수위), 프론트 `constants/stations.ts`(하드코딩된 옛 임계값)의 기준이 일부 불일치합니다. **DB값이 최종 소스**이며, 프론트 constants는 초기 목업 잔재입니다. 통일 필요.

7. **"캐시 이름에 `cctvUrls`가 있는데 실제로 쓰나요?"**
   → `CacheConfig`엔 선언돼 있지만 `CctvController`는 `@Cacheable`을 안 붙여 **현재 미사용**입니다. `aiGuide`만 실제 캐싱됩니다.

8. **"`@EnableScheduling`을 켰는데 스케줄 작업이 있나요?"**
   → 어노테이션은 있으나 `@Scheduled` 메서드는 없습니다. 확장 대비 흔적입니다.

9. **"에러 나면 사용자는 뭘 보나요?"**
   → 각 쿼리에 로딩 스켈레톤/에러+재시도 버튼 UI. AI는 실패 시 규칙기반 메시지로 대체돼 **빈 화면이 안 나옵니다.**

---

## 7. 30초 발표 오프닝 스크립트 (참고)

> "WADE는 낙동강홍수통제소의 실시간 수위 데이터를 강변 여가 이용자용 안전 정보로 번역하는 서비스입니다. 프론트는 React 19·TypeScript·TanStack Query로 서버 상태를 10분마다 자동 갱신하고, 백엔드는 Spring Boot 4·Java 21로 기상청·ITS·AI API를 통합합니다. 핵심은 '3.7m'라는 숫자를 '버스 지붕 높이 — 지금은 위험'처럼 누구나 판단할 수 있는 언어로 바꾸는 것이고, AI 브리핑이 실패해도 규칙기반으로 대체되도록 설계했습니다."

---

## 8. 빠른 자문자답 체크리스트

- Q. 서버 상태와 클라이언트 상태를 왜 나눴나? → TanStack Query(서버) / Zustand(UI).
- Q. 왜 JPA 아니고 MyBatis? → 단순 조회 위주, SQL 직접 제어가 명확.
- Q. 왜 Caffeine 캐시? → 외부 AI 호출 비용/지연 절감, 10분 캐시.
- Q. 기상청 격자좌표(nx/ny)가 뭔가? → 위경도 대신 기상청 전용 격자. DB에 관측소별 매핑.
- Q. base_time 계산이 왜 필요? → 기상청은 발표시각 기준 데이터 제공, 아직 발표 전 시각을 요청하면 빈 응답 → 가장 최근 발표시각 계산 필수.
- Q. HLS가 뭔가? → HTTP 기반 적응형 스트리밍(.m3u8). CCTV 실시간 영상 재생용, hls.js로 구현.
- Q. 이 서비스의 차별점? → 조기경보(수동 통보)와 달리 시민이 능동적으로 확인 + 숫자를 체감 언어로 번역.
```

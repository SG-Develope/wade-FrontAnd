# WADE 면접 대비 — 기술 흐름 완전 정리

> 각 기능이 "어떤 기술 · 어떤 로직 · 어떤 순서"로 만들어지고 화면에 표출되는지 코드 기준 정리.
> ⭐ 핵심은 **자바(Spring Boot) AI 안전 브리핑**. 나머지는 요약.

---

## 0. 먼저 알아야 할 사실 (문서 ↔ 코드 불일치)

- PRD/문서에는 **"AI = Claude API"** 로 적혀 있지만, **실제 코드는 Google Gemini 2.0 Flash** 를 호출한다.
  → `AiGuideService.GEMINI_API_URL = ".../models/gemini-2.0-flash:generateContent?key="`
- 면접에서는 **"Gemini 2.0 Flash를 서버 사이드에서 호출하고, 실패 시 규칙 기반(rule-based) 메시지로 폴백한다"** 고 말하는 게 정확하다.

---

## 1. 전체 아키텍처 한눈에

```
[프론트 React/Vite]  ──10분 폴링(TanStack Query)──▶  [Spring Boot API]  ──▶  외부 API
  Dashboard.tsx                                        Controller            · 낙동강홍수통제소(HRFCO) 수위
  useWaterLevels / useAiGuide                          Service               · 기상청 날씨/레이더
  AiBriefingCard 표출                                   Caffeine 캐시(10분)     · Gemini 2.0 Flash (AI)
                                                        MyBatis ─ PostgreSQL(Neon) : 관측소 기준값·장소·CCTV
```

핵심 설계 원칙 3가지:
1. **수위/날씨/AI는 실시간(10분 폴링)**, 여가장소·CCTV 메타는 정적(DB 1회 적재).
2. **상태 판정 로직(calcStatus)이 프론트·백엔드 양쪽에** 존재 → 백엔드가 status 계산해 내려주고, 프론트도 표시용으로 재계산.
3. **모든 외부 호출은 3단 방어**(외부 API → 폴백값/규칙 메시지 → 프론트 재시도 UI).

---

## 2. ⭐ AI 안전 브리핑 — 전체 흐름 (면접 핵심)

### 2-1. 한 문장 요약
> "대시보드가 관측소별 현재 수위를 모아 백엔드에 POST하면, 백엔드가 **관측소 기준값과 비교해 상태·관심수위 대비 차이·체감 높이를 계산**하고, 이 수치를 **프롬프트에 넣어 Gemini에 JSON 형식으로 요청**한다. 응답을 파싱해 `message`와 활동 가능여부(`activities`)를 반환하며, **Gemini 실패 시 동일 수치로 만든 규칙 기반 문장**으로 폴백한다. 결과는 Caffeine으로 **10분 캐싱**된다."

### 2-2. 요청이 만들어지는 곳 (프론트)

`Dashboard.tsx`
```ts
// 1) 관측소 수위 배열 → { 관측소id: 현재수위 } 맵으로 변환
const stationLevels = useMemo(() => {
  const levels = {}
  stations.forEach(s => { levels[s.id] = s.currentLevel })
  return levels
}, [stations])

// 2) 이 맵을 AI 훅에 전달
const { data: guide, isLoading, isError, refetch } = useAiGuide(stationLevels)
```

`useAiGuideQuery.ts` — TanStack Query 설정
```ts
useQuery({
  queryKey: queryKeys.aiGuide.safety(stationLevels), // 수위가 바뀌면 키가 바뀜 → 재요청
  queryFn: () => fetchAiSafetyGuide(stationLevels),  // POST /api/ai/safety-guide
  staleTime: 600_000,        // 10분간 fresh
  refetchInterval: 600_000,  // 10분마다 자동 재요청
  enabled: Object.keys(stationLevels).length > 0, // 수위 없으면 호출 안 함
  retry: 1,
})
```

`aiGuide.ts`
```ts
apiClient.post('/api/ai/safety-guide', { stationLevels }) // body: { stationLevels: {yangpo:1.7, hoguk:2.1} }
```

### 2-3. 컨트롤러 (진입점)

`AiGuideController.java`
```java
@PostMapping("/api/ai/safety-guide")
public ResponseEntity<AiGuideResponse> getSafetyGuide(@RequestBody AiGuideRequest request) {
    return ResponseEntity.ok(aiGuideService.getSafetyGuide(request.getStationLevels()));
}
```
- `AiGuideRequest` = `Map<String,Double> stationLevels` 한 필드.
- 얇은 컨트롤러 → 모든 로직은 서비스에 위임.

### 2-4. 서비스 핵심 ①: 캐싱 + 폴백 골격

`AiGuideService.getSafetyGuide()`
```java
@Cacheable(value = "aiGuide", key = "#root.target.buildCacheKey(#stationLevels)")
public AiGuideResponse getSafetyGuide(Map<String, Double> stationLevels) {
    double yangpo = stationLevels.getOrDefault("yangpo", 0.0);
    double hoguk  = stationLevels.getOrDefault("hoguk",  0.0);
    Station yStation = stationMapper.findById("yangpo"); // DB에서 기준값(관심/주의/경보/심각) 조회
    Station hStation = stationMapper.findById("hoguk");

    try {
        if (geminiApiKey != null && !geminiApiKey.isBlank()) {
            return callGeminiApi(yangpo, hoguk, yStation, hStation); // ① 우선 AI
        }
    } catch (Exception e) {
        log.warn("Gemini API 호출 실패, rule-based 메시지 사용: {}", e.getMessage());
    }
    // ② AI 실패/키 없음 → 규칙 기반 폴백
    return AiGuideResponse.builder()
        .message(generateRuleBasedMessage(yangpo, hoguk, yStation, hStation))
        .generatedAt(now())
        .cached(false)
        .build();
}
```

**캐시 키 설계 (면접 포인트)**
```java
public String buildCacheKey(Map<String, Double> levels) {
    // 관측소 정렬 후 "id:round(level*10)" 로 조합 → "hoguk:21|yangpo:17"
    return levels.entrySet().stream()
        .sorted(Map.Entry.comparingByKey())
        .map(e -> e.getKey() + ":" + Math.round(e.getValue() * 10))
        .reduce("", (a, b) -> a + "|" + b);
}
```
- `level*10` 반올림 → **0.1m 단위로 같으면 같은 캐시**. 수위가 미세하게 흔들려도 불필요한 AI 재호출을 막는다.
- Caffeine 설정(`CacheConfig`): `expireAfterWrite(10분)`, `maximumSize(50)`.
- 즉 **"수위 변화 없으면 10분간 AI 재호출 안 함"** = PRD의 'AI 응답 10분 캐싱'을 구현한 부분.

### 2-5. 서비스 핵심 ②: 프롬프트 생성 (수치 가공)

`callGeminiApi()` 앞부분 — **AI에 넘기기 전에 자바가 먼저 계산**한다.
```java
String yStatus = calcStatus(yangpo, y);      // normal/caution/warning/critical
double yDiff   = yangpo - y.getWlAttention(); // 관심수위 대비 +/- 차이
// heightLabel(yangpo) → "버스 지붕 높이" 같은 체감 표현
```

`heightLabel()` — 수치를 **체감 높이**로 번역 (PRD '체감 시각화' 차별점의 서버 구현)
```java
if (level < 0.3)  return "발목 아래";
if (level < 1.0)  return "성인 허리 높이";
if (level < 3.5)  return "버스 지붕 높이";
if (level < 5.0)  return "2층 건물 높이";
return "아파트 2~3층 높이";
```

프롬프트 골자 — **계산된 수치를 문자열로 조립**하고, **JSON만 응답**하라고 강제
```java
String prompt =
  "낙동강 실시간 수위 현황을 바탕으로 강변 여가객에게 상세한 안전 브리핑을 작성해줘.\n" +
  "[양포교] 현재 1.70m | 단계 normal | 체감 성인 남성 키 수준 | 관심수위 대비 +0.10m ...\n" +
  "[호국의다리] ...\n" +
  "아래 JSON 형식으로만 응답해. 마크다운 없이 JSON만.\n" +
  "{ \"message\": \"3~5문장, 이모지 2~3개...\", " +
  "  \"activities\": {\"walking\":true,\"fishing\":true,\"cycling\":true,\"camping\":true} }";
```
> 포인트: **AI에게 판단을 맡기지 않고, 위험 수치·상태는 자바가 미리 계산해서 넣는다.** AI는 '문장 다듬기 + 활동 가능여부 표기' 역할. → 결과의 일관성·정확성 확보.

### 2-6. 서비스 핵심 ③: Gemini 호출 & 응답 파싱

```java
// 요청 바디: Gemini generateContent 스펙
String requestBody = objectMapper.writeValueAsString(Map.of(
    "contents", new Object[]{ Map.of("parts", new Object[]{ Map.of("text", prompt) }) },
    "generationConfig", Map.of("maxOutputTokens", 800)
));

ResponseEntity<String> response = restTemplate.exchange(
    GEMINI_API_URL + geminiApiKey, HttpMethod.POST,
    new HttpEntity<>(requestBody, headers), String.class);

// 응답에서 텍스트만 추출
String text = root.path("candidates").get(0)
    .path("content").path("parts").get(0)
    .path("text").asText().trim();

// AI가 ```json ... ``` 감싸거나 잡텍스트를 붙이는 경우 방어
if (text.startsWith("```")) text = text.replaceAll("```json\\s*","").replaceAll("```\\s*","").trim();
if (text.contains("{"))     text = text.substring(text.indexOf("{"), text.lastIndexOf("}") + 1);

JsonNode parsed = objectMapper.readTree(text); // message + activities 추출
```
- **방어 파싱**이 면접 포인트: LLM이 코드펜스나 부가설명을 붙여도 `{ ... }` 구간만 잘라 JSON 파싱 → 파싱 실패 시 예외 → `getSafetyGuide`의 catch로 규칙 기반 폴백.

### 2-7. 폴백: 규칙 기반 메시지

`generateRuleBasedMessage()` — AI 없이도 **같은 수치**로 문장 생성
```java
String yStatus = calcStatus(yangpo, y);
boolean critical = "critical".equals(yStatus) || "critical".equals(hStatus);
// critical → warning → caution → 정상 4단계별 고정 템플릿에 수치 삽입
if (critical) return String.format("🚨 낙동강 심각 단계 발령! 양포교 %.2fm(관심수위 대비 %+.2fm, %s)...",
    yangpo, yDiff, heightLabel(yangpo), ...);
```
- **AI가 죽어도 서비스는 정상 동작** → PRD '연결 실패에도 안전' 원칙.

### 2-8. 표출 (다시 프론트)

`AiBriefingCard.tsx`
```tsx
isLoading ? <스켈레톤 3줄>          // "분석 중..."
: isError  ? <"불러오지 못했습니다" + 재시도 버튼(onRetry=refetch)>
: <p>{guide.message}</p>            // AI/규칙 문장 그대로 표출
```
- `activities`(walking/fishing/cycling/camping) 불리언은 여가 활동 가능여부 표기에 사용.
- 관측소 위험 시 상단 `DangerBanner`, 하단 `ActivityStrip`(장소별 이용가능/주의/이용금지)도 같은 수위 데이터로 동작.

### 2-9. 예상 질문 & 답
- **Q. 왜 AI에 수위 판정까지 안 맡기나?**
  A. 안전 정보라 정확성이 중요. 위험 단계·차이·체감 높이는 자바가 결정적으로 계산하고, AI는 자연어 표현만 담당. 그래서 폴백 규칙 메시지도 같은 수치로 일관되게 나온다.
- **Q. AI 호출 비용/속도는?**
  A. Caffeine 10분 캐싱 + `level*10` 반올림 키로 동일 상황 재호출 차단. `maxOutputTokens 800`으로 응답 크기 제한.
- **Q. LLM 응답이 깨지면?**
  A. 코드펜스 제거 + `{}` 구간 substring + JSON 파싱, 실패 시 예외→규칙 기반 폴백, 그래도 안 되면 프론트 재시도 UI. 3단 방어.
- **Q. API 키 노출은?**
  A. 서버 사이드에서만 호출. 키는 `@Value("${gemini.api-key}")`로 환경변수 주입, 프론트에 노출 안 됨.

---

## 3. 실시간 수위 파이프라인 (HRFCO)

`WaterLevelService`
- **현재 수위** `getCurrent()`: 관측소 목록 조회 → 각 관측소 HRFCO 10분 데이터 호출
  `GET {base}/{key}/waterlevel/list/10M/{obsCode}.json` → `content[0].wl` 파싱 → `calcStatus`로 상태 계산 → 임계값 전체와 함께 응답.
- **이력** `getHistory(id, hours)`: 6시간 이하는 `10M`(10분 간격), 초과는 `1H`(1시간 간격) 엔드포인트. **KST 기준** 시각을 `yyyyMMddHHmm`로 포맷, 응답을 시간순으로 `reverse`.
- **실패 처리**: 수위 못 받으면 `-1.0` 반환 → 프론트 status `unknown`; 이력 실패 시 빈 배열(앱 안 죽음).

`RestTemplateConfig` (면접에서 자주 물음)
- `@PostConstruct`로 **SSL 검증 우회**(TrustManager 전체 허용). 한국 공공 API(hrfco.go.kr)의 인증서 체인 문제(`PKIX path building failed`) 회피용.
- 타임아웃: connect 10초 / read 15초.
- ⚠️ 실무에선 전역 SSL 우회는 보안상 지양 대상 — "공공 API 인증서 이슈 임시 대응"이라고 솔직히 설명하고, 개선안(특정 호스트만 신뢰, 인증서 truststore 등록)을 덧붙이면 좋다.

---

## 4. 상태 판정 로직 (전 기능 공통)

```java
// level을 관측소 임계값과 비교 (높은 단계부터)
if (level >= wlSerious)   return "critical"; // 심각
if (level >= wlAlarm)     return "warning";  // 경보
if (level >= wlWarning)   return "caution";  // 주의보
// (AiGuideService는 wlWarning, WaterLevelService는 wlAttention 기준으로 caution 판정 — 미묘한 차이 존재)
return "normal";
```
- `Station` 도메인 필드: `wlAttention(관심) < wlWarning(주의보) < wlAlarm(경보) < wlSerious(심각) < wlFlood(계획홍수위)`.
- 이 status가 마커 색, 카드 색, 배너, AI 프롬프트까지 전부를 구동하는 **단일 판단 기준**.

---

## 5. 기타 기술 요약 (물으면 답할 정도)

- **프론트 상태관리**: 서버 데이터는 전부 **TanStack Query**(캐시·10분 폴링·placeholderData로 실패 시 직전값 유지). UI 상태(모달/패널 열림)만 Zustand/useState. → "서버상태와 클라이언트상태 분리".
- **Query Key Factory**: `queryKeys.aiGuide.safety(levels)` 처럼 키를 함수로 생성 → 오타로 인한 캐시 미스 방지, 수위 바뀌면 키 바뀌어 자동 갱신.
- **날씨**: `WeatherService`가 기상청 초단기예보(격자 nx/ny) 호출 → 대시보드 카드. 레이더/위성/태풍은 이미지 URL embed + 실패 시 SVG 폴백.
- **CCTV**: `CctvMapper`(DB) + Caffeine `cctvUrls` 캐시. 관측소 모달/카드에 스트림 표출, `HlsPlayer`로 재생, 실패 시 "영상 미제공".
- **지도**: 카카오맵 JS API. 관측소·CCTV·여가장소 마커, status별 색상(`getDotColor`).
- **배포**: FE Vercel · BE Railway · DB Neon(PostgreSQL). CORS는 `CorsConfig`.

---

## 6. 30초 엘리베이터 피치 (통째로 외우기)

> "WADE는 낙동강 수위를 '내가 가려는 장소 기준'으로 번역해주는 서비스입니다. 백엔드는 Spring Boot로 낙동강홍수통제소·기상청 API를 10분마다 수집하고, 관측소 임계값과 비교해 안전 단계를 계산합니다. 특히 AI 브리핑은, 자바가 먼저 수위·관심수위 대비 차이·체감 높이를 계산해 프롬프트를 만들고 Gemini 2.0 Flash에 JSON으로 요청한 뒤, 실패하면 같은 수치로 만든 규칙 기반 문장으로 폴백합니다. 결과는 Caffeine으로 10분 캐싱해 비용과 속도를 잡았습니다. 프론트는 React + TanStack Query로 서버상태를 폴링·캐싱하고, 상태값 하나로 지도 마커·카드·배너·AI 안내를 일관되게 표출합니다."
